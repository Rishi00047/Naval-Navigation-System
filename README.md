# Naval-Navigation-System
Naval Navigation System is a Python-based project that simulates maritime route planning between ports using graph algorithms. It calculates shortest paths using Dijkstra’s algorithm, visualizes routes on interactive maps with Folium, and simulates ship movement. It also supports route history storage using SQLite and a Flask-based web interface.
