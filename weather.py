import requests

def get_weather(city):
    # Dummy structure (you can add real API key)
    return {
        "wind_speed": "15 knots",
        "wave_height": "2m",
        "status": "Clear"
    }